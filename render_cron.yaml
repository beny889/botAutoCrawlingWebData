# Render.com Cron Service Configuration with Docker
# Tests Docker + Chrome deployment for automation system

services:
- type: cron
  name: automation-bot-docker-validation
  schedule: "0 */6 * * *"  # Every 6 hours for testing
  dockerfilePath: ./Dockerfile
  dockerContext: .
  envVars:
  - key: TELEGRAM_TOKEN
    value: "7617892433:AAHrMesr-6MosGqMq5z0JVxFKa61ZAr_abM"
  - key: TELEGRAM_CHAT_ID
    value: "-4924885979"
  - key: DISABLE_NOTIFICATIONS
    value: "true"  # Disable during validation
  - key: PYTHONUNBUFFERED
    value: "1"
  - key: DISPLAY
    value: ":99"